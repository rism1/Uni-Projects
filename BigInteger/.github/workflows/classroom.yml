    name: GitHub Classroom Workflow

    on: [push]

    jobs:
      build:
        name: Autograding
        runs-on: gtest
        steps:
          - uses: actions/checkout@v2
          - uses: education/autograding@v1
